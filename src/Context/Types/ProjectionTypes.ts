export type Projection = {
    sportsbook: string,
    name: string,
    period: string,
    playerName: string,
    values: number[],
    updated_ats: string[],
    start_time: string,
    odds: number,
    overUnder: number
}